# =========================
# Plantilla de variables .env
# Copia este archivo como `.env` y rellena tus valores
# =========================

# --- Identidad de la app ---
APP_NAME=Cobrador
APP_VERSION=1.0.0

# --- API HTTP (Laravel) ---
# URL base de tu API. Incluye /api si tu backend lo usa.
# Desarrollo:
BASE_URL=http://192.168.100.21:8000/api
# Producción (ejemplo):
# BASE_URL=https://tu-dominio.com/api

# --- Google Maps ---
# Clave de Google Maps
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# --- Tiempo Real (Laravel Reverb / Pusher o Node Socket.IO) ---
# Selecciona transporte: 'reverb' (por defecto) o 'socketio'
REALTIME_TRANSPORT=reverb
# La app prioriza: REVERB_* (cuando reverb) o NODE_WEBSOCKET_URL (cuando socketio)

# Opción A: Reverb (Laravel) por variables REVERB_*
# Clave pública Reverb/Pusher (opcional pero recomendado)
# REVERB_APP_KEY=local_key
# Host/IP donde corre Reverb (SOLO host, sin http/https)
# REVERB_HOST=192.168.100.21
# Puerto del socket (por defecto 6001 para ws, 443 para wss)
# REVERB_PORT=6001
# http => ws, https => wss
# REVERB_SCHEME=http
# Cluster Pusher (compatibilidad)
# REVERB_CLUSTER=mt1
# Endpoint de autenticación de canales privados/presencia
# REVERB_AUTH_ENDPOINT=http://192.168.100.21:8000/broadcasting/auth

# Opción B: Reverb por URL directa del WebSocket
# Formato: ws://host:puerto o wss://host:puerto
# WEBSOCKET_URL=ws://192.168.100.21:6001
# WEBSOCKET_URL=wss://tu-dominio.com:443

# Opción C: Node Socket.IO (servidor separado)
# URL base del servidor (puede ser https://dominio o https://dominio:puerto)
# NODE_WEBSOCKET_URL=http://192.168.100.21:3001
# Ruta del endpoint Socket.IO (por defecto /socket.io); cambia si tu servidor usa otro, p.e. /notify
# NODE_WEBSOCKET_PATH=/socket.io
# WEBSOCKET_ENDPOINT=/socket.io